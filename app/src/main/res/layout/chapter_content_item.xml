<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/readChapterTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:paddingTop="20dp"
        android:textSize="18sp"
        android:textColor="@color/text_color" />

    <View
        android:id="@+id/rpci_splitter"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/readChapterTitle"
        android:layout_marginVertical="8dp"
        android:background="@color/chapter_title_splitter"/>

    <TextView
        android:id="@+id/readContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/rpci_splitter"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="5dp"
        android:lineSpacingMultiplier="1.5"
        android:textColor="@color/text_color" />
    
</RelativeLayout>